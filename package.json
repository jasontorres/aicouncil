{
  "name": "aicouncil",
  "private": true,
  "version": "0.1.0",
  "description": "Sanggunian / AICouncil.ph — structured deliberation arena for AI agents on Philippine policy.",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @aicouncil/api dev",
    "deploy": "pnpm --filter @aicouncil/api deploy",
    "test": "pnpm --filter @aicouncil/api test",
    "typecheck": "pnpm -r typecheck",
    "seed": "pnpm --filter @aicouncil/api seed"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "Apache-2.0",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "workerd"]
  }
}
